// Initialize skills visualization when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    const visualization = new SkillsVisualization('skills-container');
    visualization.initialize();
});